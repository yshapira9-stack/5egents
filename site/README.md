# אתר בית הספר לצורפות — יניב וטלי שפירא

אתר סטטי (HTML/CSS/JS), עברית RTL, ללא framework. אפשר לפתוח כל קובץ `.html`
ישירות בדפדפן, או להריץ שרת מקומי לתצוגה מלאה.

## הרצה מקומית

מהשורש של הפרויקט (התיקייה שמעל `site/`):

```
node site-server.mjs
```

ואז לפתוח בדפדפן: http://localhost:3210

## מבנה

```
site/
  index.html        עמוד בית
  courses.html      קורסים
  workshops.html    סדנאות
  gallery.html      גלריה
  about.html        אודות
  blog.html         רשימת מאמרים
  press.html        כתבו עלינו (סיקור תקשורתי + המלצות בלוגריות)
  contact.html      צור קשר + מפה
  404.html          עמוד שגיאה
  blog/             עמודי המאמרים
  assets/
    css/styles.css  כל העיצוב (צבעים, פונטים, רכיבים)
    js/main.js      תפריט מובייל, הגדלת תמונות בגלריה
    images/         לוגו + תמונות
  robots.txt, sitemap.xml   SEO
```

## מה כבר אמיתי, ומה עדיין placeholder

**כבר אמיתי** (תואם את my-studio.co.il ואת `יהודה/ידע/`, אין צורך להחליף):
טלפון (`077-9968693`), וואטסאפ (`972779968693`), מייל (`info@my-studio.co.il`),
כתובת (`רחוב אילת 6, תל אביב`), שעות פעילות, וכל מחירי הקורסים/הסדנאות
שמופיעים בלי badge "⭐ בבירור".

**עדיין placeholder** — התמונות בלבד. הן נמצאות ב-`assets/images/`. כדי
להחליף תמונה, שמרו קובץ חדש **באותו שם** (למשל `bench-hero.png`) והוא יוחלף
אוטומטית בכל המקומות. שמות עיקריים: `logo.png`, `bench-hero.png` (Hero),
`ring-hero.png`, `custom-1..4.png`, `cad-sketch.png`, `ring-value.png`.

> טיפ: בכתובת `contact.html` יש גם מפת Google שמכוונת ל"אילת 6 תל אביב" —
> אם הכתובת משתנה, עדכנו את ה-`<iframe>` של המפה שם.

## לפני השקה — לאימות מול יניב/טלי

הפריטים האלה מסומנים באתר עצמו ב-badge "⭐ בבירור" (ב-`courses.html`,
`workshops.html`). לפני שהאתר עולה לאוויר כדאי לעבור איתם על יניב/טלי ולמלא:

- **מחירים חסרים:** Rhino למתקדמים, עיצוב קולקציה ומכירת תכשיטים, כל
  הסדנאות מלבד סדנת חוויה (295/790 ₪) והסדנה למשרתי מילואים (1,500 ₪).
- **מסלולים לאימות מלא (שם + מחיר + תוכן):** PRO 30, צורפות תרפויטית, צורפות
  שיקומית, צורפות דיגיטלי למתחילים.
- **עמודים שלא נבנו בכלל** (אין להם מקור תוכן זמין): השכרת סטודיו (yspace),
  עמוד "קורס עיצוב תכשיטים" עצמאי.
- **נתונים כלליים:** מספר בוגרים מדויק (כרגע "מאות", לא מספר מדויק).
- **תמונות אמיתיות** של הסטודיו/עבודות, להחלפת ה-placeholder הנוכחי.
- **המלצות נוספות:** נאסף כל מה שניתן היה לשחזר — שירה ו-Keshet Loves נוספו
  ל-`press.html`. שירלי אטיאס וגלית דיאמונד לא ניתנות לשחזור (הפוסטים
  המקוריים ב-saloona.co.il נמחקו/שבורים); אם אי-פעם יימצא לינק חדש שלהן, אפשר
  להוסיף אותו ל-`יהודה/ידע/שיווק/המלצות-תלמידים/המלצות.md` ולעדכן את העמוד.

## שינוי צבעים / פונט

הכול מרוכז בראש `assets/css/styles.css`, בבלוק `:root`:
- `--gold` — צבע האקצנט (כפתורים, הדגשות).
- `--ink` / `--bg` — שחור הטקסט והרקע.
- `--font` — משפחת הפונט.

## העלאה לאוויר (פרסום)

מכיוון שזה אתר סטטי, אפשר להעלות אותו כמעט לכל מקום. אפשרויות נפוצות:

1. **Netlify / Cloudflare Pages / GitHub Pages** (חינמי, הכי פשוט) — גוררים את
   תיקיית `site/` לאתר, מחברים דומיין, וזהו.
2. **אחסון רגיל (cPanel וכו')** — מעלים את כל תוכן `site/` לתיקיית `public_html`.

> לפני הפרסום: בקבצים יש כתובות מלאות לפי הדומיין `https://www.my-studio.co.il/`
> (בתגיות השיתוף, ב-`sitemap.xml` וב-`robots.txt`). אם הדומיין הסופי שונה —
> צריך לעדכן אותו שם.

## נגישות ו-SEO

לכל עמוד יש `<title>` ותיאור ייחודיים, תגיות שיתוף (Open Graph) לתצוגה יפה
בוואטסאפ/פייסבוק, מבנה כותרות תקין, ו-`alt` לכל התמונות. מומלץ בהמשך לחבר
Google Analytics ו-Google Search Console.
